import type { Form } from '../types/form.type.civet'

interface Props {
  form: Form
}

FormCard: React.FC<Props> := (props) =>
  { name, email, age } := props.form

  <div className="rounded-lg shadow p-3">
    <div>
      <span className="font-bold pe-2">
        Name:
      {name}
    <div>
      <span className="font-bold pe-2">
        E-mail:
      {email}
    <div>
      <span className="font-bold pe-2">
        Age:
      {age}
    
export default FormCard

